name: Build EXE

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4          # Обновил до v4
    - name: Set up Python
      uses: actions/setup-python@v5      # Обновил до v5
      with:
        python-version: '3.9'
    - name: Install deps
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Build
      run: pyinstaller --onefile --windowed --name "Messenger" main.py
    - name: Upload
      uses: actions/upload-artifact@v4   # !!! ГЛАВНОЕ: обновил до v4
      with:
        name: Messenger-EXE
        path: dist/Messenger.exe
